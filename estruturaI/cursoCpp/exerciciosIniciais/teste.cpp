#include <iostream>
using namespace std;

int main(){

    int a,b,media,n;
    float mediasGerais[n];
    float nota1,nota2,nota3,nota4;
    float peso, altura,imc;   

    /*cout << "1) A divisão 1/2 resulta em: " << 1/2 << endl;
    cout << "2) A divisão 3/2 resulta em: " << 3/2 << endl;
    cout << "3) A divisão 4/2 resulta em: " << 4/2 << endl;
    cout << "4) A divisão 3/2 + 1/2 resulta em: " << 3/2 + 1/2 << endl;
    cout << "5) A divisão -1/2 resulta em: " << -1/2 << endl;

    cout << "6) A divisão 1/2.0 resulta em: " << 1/2.0 << endl;
    cout << "7) A divisão 1 % 2 resulta em: " << 1 % 2 << endl << endl;*/

    /*cout << "Digite as idades: " << endl;
    cin >> a >> b;
    cout << "O primeiro número é: " << a << " e o segundo é: " << b << endl;
    cout << "A soma deles é: " << a+b << endl;*/

    /*media = (a+b)/2.0;

    if(media >= 18) cout << "A idade média da idade das pessoas é: " << media << " anos e eles estão aptos ao trabalho. " << endl;
    else cout << "A idade média da idade das pessoas é: " << media << " anos e eles não estão aptos ao trabalho. " << endl;*/


    /*cout << "Digite seu peso (em kg) e altura (em metros): ";
    cin >> peso >> altura;

    if(imc < 18.5) cout << "Sua classificação é Baixo Peso e tem Baixo risco de comorbidade." << endl;
    else if(imc <= 24.9 && imc >= 18.5) cout << "Sua classificação é Peso Normal e tem Médio risco de comorbidade." << endl;
    else if(imc <= 29.9 && imc >= 25) cout << "Sua classificação é Pré-obeso e tem Aumentado risco de comorbidade." << endl;
    else if(imc <= 34.9 && imc >= 30) cout << "Sua classificação é Obeso I e tem Moderado risco de comorbidade." << endl;
    else if(imc <= 39.9 && imc >= 35) cout << "Sua classificação é Obeso II e tem Grave risco de comorbidade." << endl;
    else cout << "Sua classificação é Obeso III e tem Muito Grave risco de comorbidade." << endl;*/

    /*cout << "Digite a quantidade de alunos: ";
    cin >> n;

    for(int i=0;i<n;i++){
        cout << "Digite as notas do " << i+1 << "º aluno(a): ";
        cin >> nota1 >> nota2 >> nota3 >> nota4;
        mediasGerais[i] = (nota1+nota2+nota3+nota4)/4;
    }

    int ordem;

    for(int i=0;i<n;i++){
        cout << "As médias foram:";
        cout << mediasGerais[i];
    }*/

    return 0;

}